all: build

build:
	(cd triangle && cargo build)
	(cd cube && cargo build)

update:
	(cd triangle && cargo update)
	(cd cube && cargo update)

clean:
	(cd triangle && cargo clean)
	(cd cube && cargo clean)
